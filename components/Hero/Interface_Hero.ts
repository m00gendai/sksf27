import { Medium } from "@/globals/globals_interface"

export interface HeroItem{
    image: Medium
    mainText: string
    sideText: string
    _modified: number
    _mby: string
    _created: number
    _state: number
    _cby: string
    _id: string
  }
